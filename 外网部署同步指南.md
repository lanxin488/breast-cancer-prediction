# 🌐 外网部署同步指南

## 📋 项目状态

### ✅ 本地运行正常
项目目前已在本地成功运行：
- 地址: http://localhost:5000
- 状态: 所有功能正常

### ✅ 部署配置文件就绪
项目已配置好以下平台的部署文件：

| 平台 | 配置文件 | 说明 |
|------|----------|------|
| Render | `render.yaml` | 免费，适合演示 |
| Railway | `railway.json` | 有免费额度 |
| PythonAnywhere | `wsgi.py` | 适合Python项目 |

---

## 🚀 快速部署方案

### 方案一: 使用 ngrok 临时外网访问（推荐，最快！）

**适用场景**: 答辩时需要现场外网访问，或需要快速分享

**步骤**:
```bash
# 1. 确保本地项目正在运行
# 当前运行地址: http://localhost:5000

# 2. 安装 ngrok
# Windows: 去 https://ngrok.com/download 下载

# 3. 运行 ngrok 暴露本地端口
ngrok http 5000

# 4. ngrok 会返回一个外网地址，如:
# Forwarding  https://abc123.ngrok-free.app -> http://localhost:5000
```

**优点**: 最快，无需配置，实时同步本地更改
**缺点**: 免费版有时间限制，地址会变化

---

### 方案二: Render 部署（推荐，最稳定免费）

**步骤**:

1. **注册账号**: https://render.com

2. **新建 Web Service**:
   - 点击 "New" → "Web Service"
   - 选择 "Public Git repository"
   - 或直接连接 GitHub 账号

3. **填写配置**:
   ```
   Build Command: pip install -r requirements.txt
   Start Command: gunicorn app:app
   Environment: Python
   Region: 选择最近的
   Plan: Free
   ```

4. **高级设置 (Advanced)**:
   - 添加环境变量:
     ```
     SECRET_KEY=your-secret-key-change-in-production
     ```

5. **点击 "Create Web Service"**

6. **等待部署完成**（约2-5分钟）

7. **访问你的应用**: 会获得一个类似 `https://your-app-name.onrender.com` 的地址

---

### 方案三: Railway 部署

**步骤**:

1. **注册账号**: https://railway.app

2. **新建项目**:
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 连接你的 GitHub 账号
   - 选择项目仓库

3. **自动检测配置**:
   - Railway 会自动检测 `railway.json`
   - 自动使用 Nixpacks 构建 Python 环境

4. **添加环境变量 (Settings → Variables)**:
   ```
   SECRET_KEY=your-secret-key-here
   ```

5. **等待部署完成**

6. **生成域名**:
   - 点击 "Settings" → "Domains"
   - 点击 "Generate Domain"

7. **访问地址**: 类似 `https://your-app-name.up.railway.app`

---

### 方案四: PythonAnywhere 部署

**步骤**:

1. **注册账号**: https://www.pythonanywhere.com

2. **创建新的 Web App**:
   - 点击 "Web" → "Add a new web app"
   - 选择 Python 版本（推荐 3.9+）
   - 选择 "Manual configuration"

3. **上传项目文件**:
   - 使用 "Files" 页面上传
   - 或使用 Bash 克隆 GitHub 仓库:
     ```bash
     git clone https://github.com/your-username/your-repo.git
     ```

4. **配置虚拟环境**:
   ```bash
   mkvirtualenv --python=/usr/bin/python3.9 myenv
   pip install -r requirements.txt
   ```

5. **配置 WSGI 文件**:
   - 打开 `/var/www/yourusername_pythonanywhere_com_wsgi.py`
   - 参考项目中的 `wsgi.py` 文件

6. **重启 Web App**:
   - 点击 "Reload" 按钮

7. **访问地址**: `https://yourusername.pythonanywhere.com`

---

## 🔄 本地与外网同步

### 方法一: Git + GitHub（推荐）

**优点**: 版本控制，可追溯，多人协作

**步骤**:

```bash
# 1. 在 GitHub 创建新仓库

# 2. 初始化本地 git (如果还没有)
cd "e:\专业实习期末项目\乳腺癌预测系统_软件工程作业"
git init
git add .
git commit -m "Initial commit"

# 3. 关联远程仓库
git remote add origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main

# 4. 以后更新只需:
git add .
git commit -m "Update features"
git push
```

**配置 Render/Railway 自动部署**:
- 连接 GitHub 仓库
- 开启 "Auto Deploy"
- 每次 push 后自动重新部署

### 方法二: 直接文件上传（简单）

**PythonAnywhere**:
- 使用 Web 界面的 "Files" 上传
- 或使用 Bash 的 `scp` 命令

**本地服务器**:
- 直接复制文件到服务器

---

## ⚠️ 部署注意事项

### 1. 数据库路径配置
项目已配置为 SQLite，部署时不需要额外数据库服务。

### 2. 环境变量
**生产环境必须设置** `SECRET_KEY`:
```bash
# Windows PowerShell
$env:SECRET_KEY="your-very-long-random-string"

# Render/Railway: 在环境变量设置页面添加
SECRET_KEY=your-very-long-random-string
```

### 3. 视频文件部署
- 视频文件在 `static/videos/breast_cancer.mp4`
- 确保文件大小不要太大（建议 < 50MB）
- 如果太大，考虑使用外部视频链接（如B站、优酷等）

### 4. 静态文件
Flask 自动处理 `static/` 目录下的静态文件，无需额外配置。

---

## 🎯 答辩前准备清单

### 本地演示（推荐）
- [ ] 确保项目可以本地运行: `python app.py`
- [ ] 测试所有页面功能
- [ ] 视频文件可播放
- [ ] 准备好演示数据（提前做几次预测）

### 外网部署（加分项）
- [ ] 选择一个平台部署（推荐 Render）
- [ ] 测试外网访问
- [ ] 确认所有功能正常
- [ ] 准备好外网链接

### 备份方案
- [ ] 本地运行作为首选
- [ ] ngrok 作为临时外网方案
- [ ] 录屏视频作为最后备份

---

## 📞 常见问题

### Q1: 部署后视频无法播放？
**A**: 检查视频文件路径是否正确，大小是否超过平台限制。

### Q2: 数据库不工作？
**A**: 确保 `instance/` 目录有写权限，SQLite 数据库文件会自动创建。

### Q3: 页面样式丢失？
**A**: 检查 `static/` 目录是否正确上传，静态文件路径是否正确。

### Q4: 部署很慢？
**A**: 免费平台首次部署需要安装依赖，耐心等待2-5分钟。

### Q5: 视频太大无法上传？
**A**: 
- 压缩视频（使用 HandBrake 等工具）
- 使用视频平台链接（B站、优酷等）
- 移除视频功能

---

## 🏆 建议方案

**对于答辩展示，推荐**:

1. **主要方案**: 本地运行演示
   - 最稳定，无网络依赖
   - 功能最全，响应最快

2. **加分项**: 使用 ngrok 临时外网访问
   - 展示项目可以外网访问
   - 现场演示，效果好

3. **备份方案**: 部署到 Render
   - 提前部署好
   - 备用访问地址

祝答辩顺利！🎉
